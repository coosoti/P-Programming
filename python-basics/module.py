# my_script.py

def hello(name):
    return f"Hello, {name}."

if __name__ == "__main__":
    # This code will only be executed if the script is run as a standalone program.
    name  = "Charles"
    print(hello(name))

./module.py
python3 module.py
